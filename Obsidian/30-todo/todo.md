---
title: TODO
created: 2026-06-30
tags: [todo]
status: active
---

# TODO

## Сейчас

- [x] **Веха 82 — консольная жизнь: авто-запуск vvsh + тихие логи (`log on/off`) + пакеты в README — закрыта** ([[vvsh-shell]]): «мелочи», без которых система не живая. **(1) Авто-запуск vvsh:** vsh (внешний СПАСАТЕЛЬНЫЙ шелл) при старте сам поднимает `vvsh repl` (`run vvsh repl` руками больше не нужно); вышел/упал vvsh → управление возвращается в vsh, зайти снова — `run vvsh repl`. Строку `shell` конфига не трогаем (vsh остаётся login-шеллом). **(2) Логи:** трейс ядра ([ipc]/[obj]/[mm]/[exec]/…) — это `vprintln!` под `VERBOSE` (было по умолчанию ВКЛ → сбивало вывод команд); теперь `proc::set_verbose(false)` перед интерактивной сессией (тихо по умолчанию, бут-инфо `println!` остаётся), + синкалл `SYS_LOG(on)` (№35, без гейта — отладочная удобность) и команда `log on`/`log off` в vvsh И vsh для переключения на лету. **(3) Пакеты (разобрались):** базовый консольный набор (ls/cat/grep/cd/pwd/echo/`|`/map/filter) ВСТРОЕН в vvsh — ставить не надо; больше программ — хостовой сборкой (nixpkgs `voidify` Веха 36 / uutils Веха 32) + мост `void-store-import` → корень `bin/<arch>/name`, на устройстве `run name`; on-device менеджер — будущая крупная фаза. Раздел в README «Программы и пакеты» + строка про логи. Проверено (QEMU x86 q35 + riscv-virt): бут → vvsh стартует САМ (без ввода), интерактив тих (0 строк трейса на `ls /`), `log on` → трейс появляется (4 строки на `ls /`), `log off` → снова 0, `(exit)` → vsh принимает управление. Обе арх 0 предупреждений. **Система грузится сразу в рабочий Lisp-шелл, вывод чистый.** Дальше — по вкусу: полировка (motd, авто-`log` из конфига), затем графика (п. 9).

- [x] **Веха 81 — vvsh S2c (часть 2): cd/cwd + $VAR + map/filter + line-editor — фаза ШЕЛЛА завершена** ([[vvsh-shell]], пруф №2): довели шелл до usable daily driver. **cwd/cd + относительные пути:** глобальный cwd сессии (`static CWD` под `UnsafeCell`+`AtomicUsize`, процесс однопоточный), `resolve(rel)`→абсолютный нормализованный (`.`/`..`/`//`), `cd [путь]`/`pwd` (builtins; cd проверяет что каталог, без пути — в корень), `ls`/`cat` резолвят относительный путь, приглашение показывает cwd (`vvsh:/work> `). **`$VAR` = Lisp-переменные:** в командном режиме `$name` подставляется значением Lisp-переменной (`(define name "voidfox")` → `echo hi $name` → `hi voidfox`; несвязано→пусто, как bash; единый namespace). **`map`/`filter`:** спец-формы в vvsh-core (через `self.apply`, без смены сигнатуры builtin'ов); `sf_pipe` переписан на «вжатие» `(quote acc)` → в конвейере годятся и функции, и спец-формы (`(| (list 1 2 3 4) (filter (lambda (x) (= x 2))) count)`→1). **line-editor** (свой в vvsh, vsh не трогаем): история ↑/↓ (кольцо HISTN=8), курсор ←/→/Home/End, backspace/Delete, Ctrl-C/D; `render` печатает строки без кавычек. Проверено (QEMU x86 q35 + riscv-virt): `pwd`→`/`; `cd /work`→`vvsh:/work>`; `cd ..`→`/`; `echo hi $name`→`hi voidfox`; `(map …)`→`1 4 9` построчно; `(| … (filter …) count)`→`1`; `echo recall-test`+**стрелка вверх**→повтор (история). Host-тестов 15. Обе арх 0 предупреждений, ядро не менялось. **Фаза vvsh (конфиг M1 + шелл S2) ЗАКРЫТА — vvsh это самохостящийся конфиг + интерактивный Lisp-шелл на VOID.** vsh остаётся спасательным. Дальше по роадмапу — графика (п. 9).

- [x] **Веха 80 — vvsh S2c (часть 1): конвейеры на ЗНАЧЕНИЯХ (`ls | grep vv | count`) — закрыта** ([[vvsh-shell]], пруф №2): в VOID нет захвата stdout/каналов → конвейер сделан РОДНЫМ для Lisp способом — **на ЗНАЧЕНИЯХ**, не на байтах (как PowerShell/nushell: команды отдают объекты, хост рендерит; идеально ложится на гомоиконный store — течёт `Value`, а не текст). `|` — спец-форма thread-last в vvsh-core (`(| seed этап…)`, значение течёт последним аргументом; `(| (ls "/") (grep "vv") count)` = `(count (grep "vv" (ls "/")))`); `ls` теперь ВОЗВРАЩАЕТ список имён (было: печатал); `grep` — builtin в бинаре (фильтр строк по подстроке); `length`/`count` — builtins в core; хост-рендеринг `render` (список → по элементу на строку без кавычек, `()` → ничего, прочее — каноничной формой) применяется к результату команд И выражений на верхнем уровне (смена модели с S2b: команды-данные отдают значение; `echo`/`cat` — по-прежнему прямой «терминальный» вывод). Конвейер чист (нет захвата байтов), типизирован, компонуется с любым Lisp. Проверено (QEMU x86 q35 + riscv-virt): `ls /`→файлы построчно; `(| (ls "/") (grep "vv"))`→`a.vv b.vv c.vv nope.vv`; `… count`→`4`; `(| (ls "/") (grep ".txt"))`→txt-файлы; `(| (ls "/") count)`→`11`; `(length (ls "/"))`→`11`; `(| (list 10 20 30 40) count)`→`4`. Host-тестов 14 (добавлен `length_and_pipe`). Обе арх 0 предупреждений, ядро не менялось. **`ls | grep vv | count` на VOID — но на значениях.** Плюс: в README добавлена инструкция сборки ISO для флешки (`mkboot.sh`) + тесты (собрал, работает). Дальше — S2c часть 2 (cd/cwd, переменные окружения, полировка line-editor, `map`/`filter` первого класса).

- [x] **Веха 79 — vvsh S2b: эффектные команды + синтаксис голых слов (vvsh стал ШЕЛЛОМ) — закрыта** ([[vvsh-shell]], [[0006-vvsh-lisp-config-shell]], пруф №2): REPL стал настоящим шеллом. **Гибридный ввод:** строка с `(`/`'` — Lisp (eval+печать непустого результата), иначе — команда (голые слова). **Разрешение команды:** имя связано с вызываемым (builtin/замыкание) → вызвать; связано со значением без аргументов → показать (инспекция переменной); НЕ связано → **спавн программы** с этим именем (как PATH — `hello` запускает программу hello). **Приятный итог:** trait `Host` НЕ понадобился — эффектные команды (`ls`/`cat`/`echo`/`run`) это обычные `fn(&[Value])`-builtins В БИНАРЕ, дёргают синкаллы напрямую (caps из `start_cap` ambient процессу, вывод `sys::write`), регистрируются в `shell_env()`=`root_env()`+команды; **vvsh-core остался ЧИСТЫМ** (конфиг использует `root_env` без команд). Команды работают и в Lisp-режиме и КОМПОНУЮТСЯ (`(if (= (+ 1 1) 2) (echo "ok") (echo "no"))`). Разница литерал/переменная как в bash (`echo g`→`g`; `(echo g)`→значение `g`). Проверено (QEMU x86 q35 + riscv-virt): `ls /`, `cat /shtest.txt`→`hello-from-file`, `echo привет мир`, `(define g "expr-hi")`→`"expr-hi"`, `echo g`→`g`, `(echo g)`→`expr-hi`, `hello`→спавн программы (`SYS_EXEC 'hello'`), `(if …)`→`math-ok`, `g`→`"expr-hi"` (инспекция), `nosuchcmd`→не найдена, `(exit)`→vsh. Обе арх 0 предупреждений, ядро не менялось. **vvsh — интерактивный Lisp-шелл на VOID.** Дальше — S2c (конвейеры, переменные окружения, `cd`/cwd, полировка line-editor).

- [x] **Веха 78 — vvsh S2a: интерактивный Lisp-REPL на VOID (старт фазы ШЕЛЛА) — закрыта** ([[vvsh-shell]], [[vvsh-lang]], [[0006-vvsh-lisp-config-shell]], пруф №2): начата вторая половина ADR 0006 — vvsh как интерактивный шелл-REPL поверх ТОГО ЖЕ языка, что и конфиг. **Решения:** (1) vsh остаётся ВСЕГДА как внешний спасательный шелл — vvsh НЕ заменяет его: `vvsh repl` это богатый под-шелл, в который заходишь из vsh (`run vvsh repl`) и возвращаешься по выходу/падению (упадёт vvsh — vsh ловит); (2) гибридный синтаксис (строка с `(` — Lisp, иначе команда `ls /etc`≡`(ls "/etc")`) — к S2b; (3) эффекты через trait `Host` (как `import`→`ModuleLoader`) — к S2b; (4) ни root, ни юзеров — capability-модель ([[void-no-users-root]]). **S2a:** `run vvsh repl` читает строки, вычисляет как Lisp, печатает результат, **окружение ПЕРСИСТЕНТНО между строками**. Почти не трогает vvsh-core (уже есть `Interp::eval(expr,&env)`+`root_env()`; REPL держит одно окружение сессии, `NoLoader` — эффектов пока нет); в бинаре — `cmd_repl` + минимальный `read_line` (эхо+backspace) + печать `Value` через Display; выход `(exit)`/Ctrl-D → назад в vsh. Проверено (QEMU x86 q35 + riscv-virt): `(+ 1 2)`→3; `(define x 5)`→5; `(* x x)`→25 (**x помнится**); `(define (sq n) (* n n))`→#<замыкание>; `(sq 9)`→81; `(if (= x 5) "пять" "нет")`→"пять"; `(append (list 1 2) (list 3))`→(1 2 3); `(nope)`→ошибка (не роняет REPL); `(exit)`→vsh снова принял управление. Обе арх 0 предупреждений, ядро не менялось. **Язык работает интерактивно на VOID.** Дальше — S2b (trait Host + эффектные команды `run`/`ls`/`cat` + синтаксис голых слов), S2c (конвейеры).

- [x] **Веха 77 — vvsh M1c: `rebuild` (eval→поколение) + сев `/etc/system` + `gens` — фаза КОНФИГА закрыта** ([[vvsh-lang]], [[vvsh-config-layout]], фаза vvsh, п. 8): замкнут полный цикл самохостинга конфига НА УСТРОЙСТВЕ (модель «FS-источник → поколение-сборка → бут из снимка»): правишь `/etc/system/*.vv` на VOID → `rebuild` на VOID → ребут грузит пересобранный конфиг. **Хостовый Nix для системного конфига больше не нужен.** Подкоманды `vvsh` (обвязка store — start-cap 1): `init-config` сеет модульный `/etc/system/{net,services,networking,default}.vv` (тумблер сети в `net.vv` `#t`/`#f`, `default.vv` = `(define net (import "net.vv"))` + сборка `append`+`if`); `rebuild` вычисляет `default.vv` → нормализованный текст → `obj_put` (контент-адресуемо) → корень `system/gen<N>` (N=max+1) → двигает `system/current`, **с dedup** (содержимое == текущему → «нет изменений»); `gens` перечисляет `system/gen*` + метит активное `*`. vsh получил тонкие встроенные `rebuild`/`gens`. **Решение по правам:** `OBJ_GET`/`GET_ROOT` требуют READ (dedup, чтение current), а `PUT`/`SET_ROOT`/`LIST_ROOTS` — WRITE (есть у shell'а); дал админ-шеллу READ — `store:xw`→**`store:rwx`** (init.rs `DEFAULT_GEN1/2`, nix/system.nix, сеянный default.vv), т.к. чтение поколений — работа шелла, а WRITE у него уже мощнее; код **деградирует мягко** (нет READ на старом `xw`-диске → без dedup/маркера, без падений). Бут не менялся (`init.rs::boot` читает нормализованный снимок; eval только на `rebuild` → поколение = контент-адресный кэш выхода eval). Проверено (QEMU x86 q35 + riscv-virt, СВЕЖИЙ диск): `init-config`→`gens`(gen1\*,gen2)→`rebuild`(net #t→gen3)→`gens`(gen3\*)→`rebuild` снова→**«нет изменений — уже gen3» (dedup)**→`echo #f > net.vv`→`rebuild`(net #f→gen4)→`gens`(gen4\*); **ребут** того же диска → `[init] поколение 'gen4'`, стартуют только posixfs+vsh (net-srv нет), `ping`→«сети нет» — система в ПЕРЕСОБРАННОЙ конфигурации. Обе арх 0 предупреждений. **Конфиг VOID самохостится: правка→rebuild→ребут, без хоста.** Дальше — фаза ШЕЛЛА (пруф №2): vvsh как интерактивный REPL поверх того же языка.

- [x] **Веха 76 — vvsh M1b: `import` + слияние модулей (`/etc/system/*.vv`) — закрыта** ([[vvsh-lang]], [[vvsh-config-layout]], фаза vvsh, п. 8): конфиг стал МОДУЛЬНЫМ — `default.vv` собирает систему из отдельных `.vv`, каждый возвращает свой ВКЛАД, слияние — обычным `append` (список-конкат), ровно `imports = [ … ]` NixOS, но нативно. Ключевое: `import` читает файлы, а `vvsh-core` чистый — I/O инъектируется через trait `ModuleLoader` (спец-форма `import` берёт исходник у загрузчика; бинарь `vvsh` даёт `FsLoader` поверх posixfs с резолвом относительно каталога корневого файла, host-тесты — `MapLoader` из памяти). Рефактор: `eval`+спец-формы стали методами `Interp` (несёт загрузчик + кэш импортов по имени + стек загрузки → детект циклов; модуль вычисляется в СВЕЖЕМ окружении). API: `build_config_with(src, &loader)` (M1b) и `build_config(src)` (M1a, import→ошибка). Тестов теперь 13: слияние четырёх `.vv` в gen1 точно, модуль сам считает вклад (define/if), кэш (2 импорта→1 загрузка), детект цикла (a↔b, не зависание), ошибки отсутствия/без-загрузчика. Проверено (QEMU x86 q35 + riscv-virt): `/etc/system/{services,networking,shell}.vv` + `default.vv` с тремя `import` внутри `(append …)` → `run vvsh eval /etc/system/default.vv` печатает ТОЧНО gen1 (три строки), `networking.vv` сам вычислил вклад через `define`/`if`; импорт отсутствующего модуля → чистая ошибка (код 1). Обе арх 0 предупреждений, ядро VOID не менялось. **Конфиг — из модулей, сливаемых на VOID.** Дальше — M1c (`rebuild`→коммит поколения, сев `/etc/system`, бут из снимка, `gens`).

- [x] **Веха 75 — vvsh M1a: системный конфиг ВЫЧИСЛЯЕТСЯ на VOID (Lisp reader→eval→нормализация) — закрыта** ([[vvsh-lang]], [[0006-vvsh-lisp-config-shell]], [[vvsh-config-layout]], фаза vvsh, п. 8): первый шаг самохостинга конфига — `.vv`-файл читается из posixfs, вычисляется **на самом VOID** и печатается нормализованным системным конфигом (те же строки `service …`/`shell …`, что даёт `nix/system.nix` и читает `init.rs::apply`) — хостовый Nix для ЭТОГО больше не нужен. **A2** (решение ADR): язык — в общем `no_std`+`alloc` крейте `libs/vvsh-core` (reader S-выражений + значения на `Rc` + метациркулярный вычислитель со спец-формами `quote/if/define/lambda/let/cond/begin/and/or` + встроенные `list/append/cons/car/cdr/=/+/-/*/service/shell/system` + нормализатор), потребитель — программа `bin/<arch>/vvsh` (свой bump-`global_allocator` поверх `heap_map`; ядро крейт НЕ линкует). Крейт **host-тестируем** (`#![cfg_attr(not(test), no_std)]`): 7 тестов `cargo test` без QEMU, включая эквивалентность gen1 (`net #t`) и gen2 (`net #f`) байт-в-байт. `run vvsh eval FILE` берёт posixfs-endpoint из `start_cap(0)` (наследство vsh, как install.rs), `stat` до `open` (опечатка не плодит пустышку). Проверено (QEMU, x86 q35 + riscv-virt): `(system (service "posixfs" "store:rw") (shell "vsh" "endpoint:posixfs" "env"))` → две точные строки; `(define net #t)(system (if net (service "net-srv" …) (list)))` → ветка then; `(define n #f)(system (if n (service "a" …) (service "b" …)))` → `service b store:rw` (**ветка else — `if` реально вычисляется, не эхо**); несуществующий путь → чистая ошибка. Обе арх 0 предупреждений, ядро VOID не менялось. **Конфиг из ДАННЫХ стал ПРОГРАММОЙ, вычисляемой на VOID.** Дальше — M1b (`import`+слияние `/etc/system/*.vv`), M1c (`rebuild`→коммит поколения, сев `/etc/system`, бут из снимка, `gens`).

- [x] **Веха 74 — install стал ОТДЕЛЬНОЙ программой (`run install`), которой нет на установленной системе — закрыта** ([[install]]): раньше `install` был встроенной командой vsh → присутствовал в КАЖДОЙ системе, даже уже установленной (бессмысленно и опасно — «Стереть диск» под рукой навсегда). Стал программой `bin/<arch>/install`, которую init сеет ТОЛЬКО при загрузке с install-носителя (есть загрузочный модуль с образом диска — `arch::boot_module().is_some()`, тот же признак «я установочный носитель», что и раньше). После установки система грузится с диска БЕЗ модуля → install не сеется, а прежний корень снимается (`del_root`) → команды установки на рабочей системе просто нет. Права: у `SYS_EXEC`-ребёнка (`run`) a0/a1 НЕ несут стартовые права (в отличие от init-спавна) — install.rs читает store-cap через `SYS_STARTCAP` (`start_cap(1)` = `store:xw`, есть WRITE, которого требует `SYS_INSTALL`). Наше: `install.rs` (новая программа), `build.rs`+`main.rs` (регистрация + условный сев по boot_module + `del_root` иначе), `vsh.rs` (убрана встроенная `install` и строка в help). Проверено (x86, QEMU): (а) обычная `-kernel`-загрузка (boot_module None) → seed печатает 19 корней, `install` нет ни в `roots`, ни в `help`; (б) загрузка с install-ISO (boot_module Some) → seed «(+install — носитель)», `run install` → «VOID установлен на диск». Регрессии: обе арх собираются, 0 предупр. **Установка теперь есть ровно там, где нужна — на носителе, и нигде больше.**

- [x] **Веха 73 — PING через ПОРТИРОВАННЫЙ e1000 (ARP + ICMP, DMA + IRQ) — закрыта** ([[lx-e1000-ping]], dde_linux-конвейер, ТОЧКА ДОКАЗАТЕЛЬСТВА): неизменённый Linux-драйвер e1000 (`e1000_hw.c`/`e1000_main.c`/`e1000_param.c`, Linux 6.18.7 verbatim) на VOID делает **полный IP round-trip до живого шлюза** — ARP узнаёт MAC шлюза → ICMP echo request → приём echo reply, всё на реальном DMA и по прерыванию карты. **Способность VOID хостить неизменённые Linux-драйверы — доказана.** Наше (добавка к `drv_e1000.c`, сам L3/L4 собираем — стек это net-srv, мост дальше): хелперы `inet_csum` (RFC 1071), `build_icmp_echo` (Ethernet+IPv4+ICMP+32 Б payload, обе к/суммы), `e1000_tx(hw,tx,idx,dma,len)` (дескриптор+`ew32(TDT)`+ждать DD), `e1000_wait_rx(rx,&from)` (ждать следующий DD ПО ПРЕРЫВАНИЮ, сканируя с `from`, пропуская пороговые прерывания); стадии — (1–2) bring-up+кольца+IRQ (69–72), (3) ARP TX→wait_rx→`sha`=MAC шлюза, (4) ICMP echo к 10.0.2.2 → `e1000_tx` desc 1 → ждём echo reply, пропуская чужие кадры (проверка IPv4+ICMP+type=0+src=шлюз). TX/приём — тот же тракт колец на DMA-cap + `vsys_irq_wait`+ISR+completion (Вехи 70–72); Веха 73 добавляет лишь сборку/разбор пакетов. Проверено (x86, QEMU q35 `-device e1000,mac=…99` + virtio-net ядру): `TX ARP → DD=1`, `ARP-ОТВЕТ: 10.0.2.2 = 52:55:0a:00:02:02 (IRQ=2)`, `TX ICMP echo request → 10.0.2.2 (74 Б) → DD=1`, **`ICMP ECHO REPLY от 10.0.2.2: id=0x1234 seq=1 ttl=255 (IRQ=3)`**, `Результат: PING 10.0.2.2 через НЕИЗМЕНЁННЫЙ e1000 (DMA+IRQ, ARP+ICMP) — OK`. Регрессии: x86 без `-device e1000` — не спавнится, ping через virtio-net (7143 мкс) ок; riscv-virt — не спавнится, vsh+ping ок (941 мкс); обе арх 0 предупр. Ядро VOID не менялось. **Дальше драйверы — ПО ТРЕБОВАНИЮ** (X54C Atheros/EHCI/wifi/GPU тем же конвейером, когда нужен чип); опц. полировка e1000 — штатный `e1000_probe`→`open`→`up` + мост к net-srv. Следующая крупная фаза — **vvsh** (п. 8).

- [x] **Веха 72 — портированный e1000: RX по ПРЕРЫВАНИЮ (`request_irq`↔`vsys_irq_wait`) — закрыта** ([[lx-e1000-irq]], dde_linux-конвейер, IRQ): Веха 71 приняла ARP-ответ **опросом** DD; эта — тот же приём, но **по реальному прерыванию карты**: драйвер спит, пока e1000 не поднимет INTx. Замыкает фундамент userspace-драйверов (MMIO+DMA+**IRQ**) на портированном коде. **Модель** (Lx_kit — один поток, нити под IRQ как в lx_emul не годятся): доставку IRQ вшили в **idle-путь планировщика** (Genode: EP спит на сигнале, просыпается по IRQ) — `lx_sched_run` в простое, если нет готовых задач/таймеров, но зарегистрирован IRQ, зовёт `vsys_irq_wait(cap)` (`SYS_IRQ_WAIT`, [[irq]] Веха 52): весь процесс блокируется в ядре до прерывания, по возврату зовёт handler в softirq-контексте (`sched_current==NULL`) — тот будит задачу `complete()`. API `lx_irq_register/unregister` в `lx_sched.h`; компилируется и в «вычислялке» (Веха 68, без syscall.h): никто не регистрирует → idle не трогает, `vsys_irq_wait` только под `LX_HAVE_SYSCALL`. Level-triggered+oneshot (Веха 52): кадр до взвода пина не теряется. Наше: `lx_net.c` — `request_irq` из no-op стал настоящим (регистрирует handler при наличии IRQ-cap; каст `irq_handler_t`→`lx_irq_handler_t`), `lx_net_set_irq_cap`; `drv_e1000.c` — ISR `e1000_isr` читает ICR (R/clr гасит INTx), при RX-причине `complete(&g_rx_done)`; bring-up `init_completion`+`request_irq`+`ew32(IMS, RXT0|RXDMT0|RXO)`; приём — `wait_for_completion` в цикле (карта даёт пороговое RXDMT0 ДО кадра — ждём, пока не появится DD; `reinit_completion`); `main()` берёт IRQ-cap `vsys_start_cap(2)`. Проверено (x86, QEMU q35 `-device e1000,mac=52:54:00:12:34:99` + virtio-net ядру): `выданы MMIO+DMA+IRQ cap`, `IRQ-cap есть`, `TX ARP-запрос → DD=1`, **`проснулись по ПРЕРЫВАНИЮ карты: обслужено IRQ=2, ICR=0x00000090`** (0x90=RXT0|RXDMT0 — реальные RX-причины), `ARP-ОТВЕТ от 10.0.2.2: MAC шлюза 52:55:0a:00:02:02`, `Результат … RX ARP-ответ ПО ПРЕРЫВАНИЮ … OK`, `SYS_EXIT(0)`. **Задача продолжилась ТОЛЬКО из-за реального прерывания карты**, не опроса. Регрессии: x86 без `-device e1000` — драйвер не спавнится, `ping 10.0.2.2` через virtio-net ок; riscv-virt e1000 нет — не спавнится, vsh+ping ок (236 мкс); `lx_e1000_port` (Веха 68) собирается обе арх. Обе арх: 0 предупреждений. Ядро VOID не менялось. **Портированный драйвер принимает трафик по прерыванию на живом железе.** Дальше — штатный ISR/NAPI (vendored `e1000_intr`/`e1000_clean` через `e1000_probe`→`e1000_open`→`e1000_up`) + мост RX/TX↔net-srv → `ping` через ПОРТИРОВАННЫЙ e1000.

## Дальше (порядок принят, но может поменяться)
Пакетная дорожка [[void-pkg]] пройдена (A/B/C); декларативный init, железо (загрузка+клавиатура+**диск AHCI**+**установка на диск**+**сеть e1000**+**USB xHCI-клава**+**userspace-драйверы: MMIO+DMA+IRQ**+**lx_emul-каркас Rust+C**+**первый реальный .c ядра Linux**), косметика TTY, папки, редактор строки и освобождение фреймов взяты. Осталось (по [[known-gaps]]):
7. [x] **К Linux-драйверам — фундамент + доказательство закрыты** ([[lx-linux]], конвейер dde_linux). От неизменённого `.c` ядра Linux (Веха 55) через полный **`Lx_kit`**-рантайм — аллокатор/`list`/`bitops`/`err`/`io`/`delay` + кооперативный планировщик (задача=стек+setjmp/longjmp, один поток, модель Genode) + jiffies/таймеры + wait/completion + workqueue + driver-model + шина PCI (Вехи 56–67) — до **самого драйвера e1000** (Вехи 68–73: компиляция → MMIO → DMA-TX → DMA-RX/ARP → IRQ → **`ping` до живого шлюза**). Детали по вехам — в заметках `lx-*` ([[lx-kit]]…[[lx-pci]], [[lx-e1000-port]]…[[lx-e1000-ping]]); фундамент userspace-драйверов — [[userspace-drivers]]+[[irq]], каркас lx_emul — [[lx-emul]]/[[lx-emul-c]]. **✅ Способность VOID хостить неизменённые Linux-драйверы ДОКАЗАНА** (компиляция→исполнение→MMIO→DMA-TX→DMA-RX→IRQ→ping; ядро VOID не менялось) — см. [[void-driver-hosting-proven]]. **Дальше драйверы конкретного железа — ПО ТРЕБОВАНИЮ** (X54C Atheros/Realtek NIC, EHCI USB, wifi ath9k/mac80211 «гора», GPU DRM+Mesa «бо́льшая гора» — тем же конвейером, когда нужен живой чип, не линейным списком). **Опц.** полировка e1000 (НЕ обязательна для доказательства): штатный `e1000_probe`→`e1000_open`→`e1000_up` (vendored ISR `e1000_intr`+NAPI `e1000_clean` по [[lx-pci]]) + мост RX/TX ↔ `net-srv` ([[virtio-net]]) → `vsh ping` через портированный драйвер как общий backend. Своими силами ещё: NVMe, UEFI-GOP, TCP+DHCP; riscv — device tree + eMMC (VisionFive 2). **Следующая крупная фаза — vvsh (п. 8).**
8. [ ] **vvsh — Lisp VOID для конфига и шелла** ([[0006-vvsh-lisp-config-shell]], фаза после e1000→ping): маленький Lisp/S-выражения, вычисляемый НА VOID (самохостинг — уводит eval конфига с хостового Nix), результат контент-адресуем; гомоиконный, Тьюринг-полный НАМЕРЕННО (гибкость без потолка). Разделение: язык вычисляет → init грузит нормализованные данные. Двойная роль: (а) конфиг системы вместо Nix-на-хосте (Веха 40 [[declarative-init]]); (б) движок `vsh` — интерактивный шелл-REPL с конвейерами/скриптами БЕЗ порта bash (bash дорог: fork/сигналы/job-control на персистентном микроядре). Инкрементально: парсер S-выражений+примитивы → метациркулярный вычислитель → стдлиб → интеграция в init и vsh. Nix остаётся для ПАКЕТОВ ([[0004-void-pkg]]). **Фаза КОНФИГА (M1) закрыта: M1a (Веха 75), M1b (Веха 76), M1c (Веха 77) — правка `/etc/system/*.vv`→rebuild→ребут, хостовый Nix для конфига не нужен ([[vvsh-lang]], [[vvsh-config-layout]]). Фаза ШЕЛЛА ([[vvsh-shell]]) ЗАКРЫТА: S2a (Веха 78 — Lisp-REPL), S2b (Веха 79 — команды + голые слова + спавн программ), S2c (Вехи 80–81 — конвейеры на ЗНАЧЕНИЯХ + cd/cwd + $VAR + map/filter + line-editor). **Вся фаза vvsh (конфиг + шелл) закрыта.** vsh — спасательный внешний шелл. Следующая крупная фаза по роадмапу — графика (п. 9).**
9. [ ] **Графика — выход из терминала в графическую оболочку** (будущая КРУПНАЯ поздняя фаза, [[0007-graphics-native-compositor]], решения приняты): портировать Wayland+niri — против шерсти (сокеты/`SCM_RIGHTS`/DRM+Mesa) → **делаем СВОЙ стек**. Композитор-сервер на capability-IPC, окно = разделяемый буфер-объект + cap, свой крошечный протокол; **свой тулкит** (immediate-mode Rust, пишем сам — не egui); **софт-рендер сначала**, GPU-ускорение потом (целимся на **AMD**, не Nvidia; сроков нет); FB — простейший путь «увидеть, что работает» (GOP/virtio-gpu) + мышь; окна сперва плавающие → со временем **тайлинг «как niri»**; **персистентный GUI** (окна переживают ребут, наследие [[checkpoint]]) — определяющая фишка. Предпосылки: пиксельный FB + мышь. Первый proof (как с e1000): FB + мышь + одно окно, подвигать мышью.
- [ ] Установка: GPT/UEFI, выбор диска, обновление на месте (сейчас только полное стирание) — [[install]]. Сам запуск установки уже вынесен в отдельную программу `run install` (Веха 74), присутствует только на носителе.
- [ ] Крупное: TCP (настоящая сеть), права/времена в posixfs, `rm -r`.
- [ ] Ещё «фишки NixOS» (когда захочется): окружения-как-объекты (аналог nix-shell), «flake» = дерево content-id.

---

# VOID — staged роадмап

Принцип: радикальные идеи включаются по одной, **каждая за работающей вехой** — чтобы всегда
было что-то живое в QEMU, а не вечная стройка. Первые ~5 слоёв одинаковы для любого ядра и не
зависят от стержневого тезиса ([[0002-persistent-content-addressed-capability-core]]) — это
чистое «понять, как всё работает». Радикальное начинается со слоя памяти/хранилища.

## Фаза 0 — фундамент (от железа вверх)
- [x] **Веха 1.** Boot: OpenSBI → entry asm → стек → `kmain`; вывод в UART (NS16550).
- [x] **Веха 2.** Trap/прерывания: обработчик исключений (ebreak), таймерные прерывания (S-mode), дамп регистров при фатале. См. [[traps-and-timer]].
- [x] **Веха 3.** Виртуальная память: Sv39-пагинг, direct map RAM + W^X ядра, переход на трансляцию. См. [[sv39-paging]].
- [x] **Веха 4.** Kernel heap: free-list аллокатор + `#[global_allocator]`, арена из `frame::reserve`, `alloc` работает. См. [[kernel-heap]], [[memory-regions]].
- [x] **Веха 5.** Многозадачность: контексты + кооперативный round-robin планировщик. См. [[scheduling]].
- [x] **Веха 5.5.** Вытеснение (preemption) по таймеру: переключение из trap'а, состояние прерываний на задачу, атомарный вывод. См. [[scheduling]].

## Фаза 1 — стержень (тут начинается VOID)
- [x] **Веха 6.** Объектная модель в RAM: неизменяемые значения по хэшу + дедуп + изменяемые корни/история версий. См. [[object-model]], [[persistence-and-volatility]].
- [x] **Веха 7.1.** Драйвер virtio-blk: чтение/запись секторов (virtio-mmio, split virtqueue, опрос). См. [[virtio-blk]].
- [x] **Веха 7.2.** Персистентный store: суперблок + A/B индекс + append-only объекты; корень переживает перезагрузку. См. [[persistent-store]].
- [x] **Веха 8.** Capability: непод­делываемые дескрипторы (слот+поколение), c-space на домен, аттенуация при grant, отзыв. См. [[capabilities]].
- [x] **Веха 9.** Async-executor: future-задачи + waker + async-канал поверх объектного store. См. [[async-executor]].

> **Фаза 1 закрыта:** все три ингредиента ADR 0002 (персистентность · контент-адресация · capability) + async-модель исполнения над ними собраны и работают в QEMU.

## Фаза 2 — наружу
- [x] **Веха 10.1.** U-mode + граница syscall (`ecall`): страницы `U`, переключение стека через `sscratch`, SYS_WRITE/SYS_EXIT, вход/возврат через `context_switch`. См. [[user-mode]].
- [x] **Веха 10.2a.** Процессы со своим `satp` (клон корня ядра + приватный стек) + кооперативное планирование (SYS_YIELD/EXIT), `enter_user_frame`. См. [[processes]].
- [x] **Веха 10.2b.** IPC (RECV/CALL/REPLY, синхронный rendezvous) + первый userspace-сервер (удвоение) и клиент. См. [[ipc]].
- [x] **Веха 11.** Драйвер блоков как userspace-сервер: IPC с передачей буфера (CALL/REPLY), шлюз `SYS_BLK_READ`, сервер+клиент читают сектор. См. [[block-driver]].
- [x] **Веха 12.** Capability-защищённые IPC-эндпоинты: домен на процесс, `CALL` по cap на эндпоинт (`SEND`), `BLK_READ` по cap на устройство (`READ`) — доступ к серверу/железу только по дескриптору. См. [[ipc-endpoints]].
- [x] **Веха 13.** Сервер объектного store в userspace: `CALL` с буфером запроса + шлюзы `OBJ_PUT`/`OBJ_GET` под cap на `Store`; клиент кладёт/читает объекты по IPC, не касаясь store. См. [[object-store-server]].
- [x] **Веха 14.** Именованный корень как capability: `OBJ_SET_ROOT`/`OBJ_GET_ROOT` по IPC под store-cap; объект, привязанный к корню, переживает перезагрузку (persistence через userspace-сервер). См. [[persistent-root-ipc]].
- [x] **Веха 15.** reply-capability: `RECV` выдаёт одноразовый reply-cap (`Target::Reply`), `REPLY` требует его и отзывает — нельзя ответить тому, кто не звал/подделать. См. [[reply-capability]].
- [x] **Веха 16.** Вытеснение процессов: `sie.STIE` вкл на сессию, таймерный trap из U = переключение процесса (`preempt_tick` + `next_runnable`), ядро с `SIE=0` не вытесняется. См. [[process-preemption]].
- [x] **Веха 17.** `BLK_WRITE`: шлюз записи сектора под правом `WRITE` на устройство; блок-протокол кодирует сектор+флаг записи в `op`. Клиент пишет и читает сектор обратно через сервер. См. [[block-driver]].
- [x] **Веха 18.** Слой совместимости — POSIX-персоналия как сервер (под-вехи 18.1–18.4, **закрыта**).
    - [x] **18.1.** Сервер-персоналия + POSIX-shim: open/write/close/read, namespace в RAM сервера, без изменений ядра. См. [[posix-personality]].
    - [x] **18.2.** Персистентность файлов через store: содержимое под корнем-именем (роты хранилища = директория), переживает перезагрузку. См. [[posix-personality]].
    - [x] **18.3.** `stat`/`unlink`/`readdir`, режимы open (`O_APPEND`/`O_TRUNC`), смещения на дескриптор; персистентный индекс каталога `.dir`; шлюз ядра `OBJ_DEL_ROOT` (честный unlink — корень уходит в GC). См. [[posix-personality]].
    - [x] **18.4.** Программа `mini-sh` на чистом POSIX-shim (libc-заглушка `sh_open/read/write/close/readdir`, `mini-cat`/`echo`, stdout=fd 1 → консоль): в теле программы ни `ecall`, ни op-кодов, ни capability — прозрачность личности. См. [[posix-personality]].

## Фаза 3 — самостоятельность (см. [[0003-phase-3-exec-by-hash]])
Центральный примитив фазы: **программа = контент-адресуемый объект, exec по ContentId** — как derivation в Nix, прямое следствие ADR 0002.
- [x] **Веха 19.** Программа как объект store: ELF-крейт `programs/hello` → байты в store под корнем `bin/hello` → минимальный ELF-загрузчик (PT_LOAD, W^X) → `spawn_elf` по ContentId; на втором запуске ELF грузится с диска. См. [[exec-from-store]].
- [x] **Веха 20.** UART-ввод по прерыванию (кольцевой буфер, вычерпывание на тиках) + `SYS_READ`/`SYS_EXEC` (право `EXEC`, foreground-ожидание) + интерактивный `vsh`: `ls`/`cat`/`echo > f`/`run bin/hello`/`exit`; смена корня из userspace = атомарный чекпойнт. См. [[interactive-shell]].
- [x] **Веха 21.** Cap-transfer по IPC (grant-в-сообщении, право GRANT; `CAP_DERIVE` — аттенуация) + персистентный c-space (`.cspace` в store, чекпойнт на каждую передачу, restore с точными поколениями) — capability переживают перезагрузку, тезис ADR 0002 полный. См. [[ipc-cap-transfer]].
- [x] **Веха 22.** Куча процесса (`SYS_MAP`, ленивые страницы, доотображение под шлюзы); user page fault убивает процесс, а не ядро; лимиты POSIX-персоналии сняты (16 файлов × 4 КиБ, метаданные в куче). См. [[process-heap]].

> **Фаза 3 закрыта:** система самостоятельна — программы загружаются из store по content-id,
> есть живой shell с вводом, capability передаются по IPC и переживают перезагрузку вместе с
> c-space, у процессов настоящая ленивая память и честные фолты.

## Фаза 4 — мультиархитектурность и пакеты (см. [[0004-void-pkg]])
Цель: «поставил на устройство и пользуешься» — ядро на двух архитектурах из одного дерева
(`cargo build --target …`, код разводится модулем `arch/` по узкому контракту), программы —
из настоящих пакетных баз через резолвер void-pkg (A→B→C). NixOS доказала стабильность этой
модели поверх чужого фундамента; у VOID фундамент (контент-адресуемый store) — родной, в ядре.
- [x] **Веха 23.** Userspace целиком в ELF из store: крейт `programs/user` (либа syscall-шимов + 13 бинарей), сев по хэшу под корни `bin/<имя>` (актуально/посеяно/обновлено), секция `.user` похоронена (мины memset/jump-таблиц сняты — ядро на `opt-level=2`), все шлюзы доотображают ленивые кучи. См. [[elf-userspace]].
- [x] **Веха 24.** Граница архитектур: `arch/{riscv64,x86_64}` за узким контрактом (ADR 0005) — оба таргета из одного дерева, ноль предупреждений; x86 — заглушка (COM1 + примитивы прерываний настоящие, остальное `unimplemented!` с обязательствами bring-up). См. [[arch-boundary]].
- [x] **Веха 25.** x86_64 оживает: PVH direct boot (вместо Limine — VA==PA и `cargo run` без внешних загрузчиков), трамплин 32→64, IDT, PML4 W^X, LAPIC-вытеснение, контексты — ядерная половина демо на обоих таргетах. См. [[x86-bringup]].
- [x] **Веха 26.** Userspace x86_64: ring3 (GDT/TSS) + `int 0x80` + iretq, `restart()`/`ARCH_NAME` в контракте, PDPT-клон пространств, ввод опросом на тиках, программы под обе архитектуры (модуль `abi`), корни `bin/<arch>/<имя>` + миграция. Демо-паритет (кроме диска). См. [[x86-userspace]].
- [x] **Веха 27.** Устройства x86_64: транспорт virtio в контракте arch (mmio/pci), virtio-blk-pci + MSI-X, IOAPIC для консоли — персистентность на обеих архитектурах, один диск с бинарями и файлами двух архитектур. См. [[x86-devices]].
- [x] **Веха 28.** Микробенчи и отчёт памяти против Linux-гостя в том же QEMU; корневой README («это уже система», цифры, не-претензии). Заодно закрыла Bench-веху из «Когда-нибудь» (crash-тест A/B случился внепланово ещё в Вехе 27).
- [x] **Веха 29.** Мост host→store: `libs/void-store` (формат как библиотека, `BlockIo`), `void-store-import` (put/nar/ls/cat), NAR-импорт `nix-store --dump` → корни store; форк rust сабмодулем под std-порт. См. [[store-bridge]].
- [x] **Веха 30.** Контракт запуска (ABI v2): argv в SYS_EXEC, env + стартовые capability наследуются (`cap::endow`), SYS_ARGS/SYS_STARTCAP; posixfs seek/rename; vsh run-с-аргами/mv/tail. См. [[process-contract]].
- [x] **Веха 31.** std-порт: таргеты *-unknown-void + pal в форке rust; тулчейн void из stage1; hello-std обычным cargo — argv/env/куча/HashMap/exit-код на обеих архитектурах. См. [[std-port]].
- [x] **Пакетная дорожка (бэкенд A) закрыта.** Веха 30 (контракт запуска) → Веха 31 (std-порт из `vendor/rust`) → Веха 32 (uutils) → Веха 36 (nixpkgs-cross через void-libc); профили/поколения/откат — свойства store, не менеджера.

## Фаза 5

- [x] Веха 18: слой совместимости (POSIX-персоналия как сервер) — **закрыта** (18.1–18.4). См. [[posix-personality]].
- [x] Анализ состояния после Фазы 2: разрывы до работоспособности, SASOS, цена x86-порта. См. [[phase-2-state-analysis]]; решение — [[0003-phase-3-exec-by-hash]].
- [x] **Фаза 3 — самостоятельность — закрыта** (Вехи 19–22): exec по content-id ([[exec-from-store]]), интерактивный `vsh` ([[interactive-shell]]), capability по IPC + персистентный c-space ([[ipc-cap-transfer]]), куча процесса + честные фолты ([[process-heap]]). Под-вехи — в роадмапе ниже.
- [x] Стратегия пакетов и самодостаточности принята — [[0004-void-pkg]]: резолвер void-pkg с бэкендами A→B→C (native/nix-cross → linux-abi → wasi), Rust-шорткат (std-порт + uutils), host-мост в store. Развёрнутый план — [[void-pkg]].
- [x] **Веха 23 — userspace целиком в ELF из store — закрыта** (старт Фазы 4): все 13 программ — ELF-бинари крейта `programs/user` под корнями `bin/<имя>`, сев по хэшу (обновление системы = смена корня), секция `.user` похоронена, ядро на `opt-level=2`, шлюзы честны к ленивым кучам. См. [[elf-userspace]].
- [x] **Веха 24 — граница архитектур — закрыта**: `arch/{riscv64,x86_64}` за узким контрактом ([[0005-multiarch-arch-boundary|ADR 0005]]) — trap-кадр методами, UserTrap вместо scause, токены пространств, политики масок; оба таргета собираются из одного дерева без предупреждений (x86 — заглушка с COM1 и «обязательствами» bring-up); riscv-регрессия чиста. См. [[arch-boundary]].
- [x] **Веха 25 — x86_64 оживает — закрыта**: PVH direct boot вместо Limine (сохраняет VA==PA и `cargo run` — [[x86-bringup]]), трамплин 32→64, IDT + фатальные дампы, PML4-пейджинг W^X (NX), LAPIC-таймер с вытеснением, контексты ядерных задач — ядерная половина демо (store в RAM, cap, sched, async, GC, commit) идёт на x86; riscv-регрессия чиста.
- [x] **Веха 26 — userspace x86_64 — закрыта**: ring3 (GDT DPL3 + TSS.rsp0-как-sscratch), syscall `int 0x80`, вход iretq, классификация ring3-трапов (#PF по err-битам, EOI до планировщика); контракт пополнен `restart()` (откат rip — riscv-у бесплатно) и `ARCH_NAME`; клон пространства копирует PDPT (PML4[0] делит ядро и user); ввод консоли — опросом LSR на тиках (vsh интерактивен без IOAPIC); программы собираются под обе архитектуры (арх-модуль `abi` шимов), корни — `bin/<arch>/<имя>` с миграцией легаси. Полный демо-паритет, паник 0; riscv-регрессия чиста. См. [[x86-userspace]].
- [x] **Веха 27 — устройства x86_64 — закрыта**: транспорт virtio — ось контракта (`probe_virtio_blk` отдаёт mmio/pci, virtqueue общий); PCI ровно на диск (CF8/CFC, vendor-caps, BAR от SeaBIOS, `map_mmio` в рантайме); прерывание диска — **MSI-X** прямо в LAPIC (обход PIRQ-болота INTx), консоль — IOAPIC GSI4 → честный сон до ввода; device-IRQ обрабатываются из любого кольца (кадр цел + iretq). **Один диск — две архитектуры**: riscv↔x86 читают файлы друг друга, корни `bin/<arch>/*` бок о бок, c-space общий; внеплановый crash посреди бота подтвердил атомарность A/B-индекса. См. [[x86-devices]].

- [x] **Веха 28 — микробенчи и отчёт памяти — закрыта**: `bin/bench` (rdtime/rdtsc из U-mode, `scounteren`), глушение трассировки шлюзов на замер (`vprintln!`), зеркальный бенч Linux-гостя в том же QEMU (`Code/bench/bench-linux.rs`); цифры и оговорки — в корневом README (IPC ×7, объект ×9.6, exec ×12 быстрее Linux; null-syscall ×3.4 медленнее; ~5 МиБ против ~80 МиБ). Корневой README написан — «это уже система» с честными критериями и не-претензиями.
- [x] **Веха 29 — мост host→store — закрыта** (старт пакетной дорожки [[0004-void-pkg]]): store вынесен в `libs/void-store` (no_std, трейт `BlockIo`) — один формат/одна реализация на ядро и хост; `tools/void-store-import` (ls/cat/put/nar) кладёт файлы и NAR-архивы в образ диска без пересборки ядра, обновляя `.dir` посикс-персоналии; `nix-store --dump → nar → vsh cat` проверен на обеих архитектурах, взаимные коммиты хост↔ядро консистентны. Форк rust 1.97.0 — сабмодуль `vendor/rust` (ветка `void`) под Веху 31. См. [[store-bridge]].

- [x] **Веха 30 — контракт запуска процесса — закрыта** (ABI v2, фундамент std): `SYS_EXEC` несёт argv (NUL-блоб ≤512 Б, argv[0] ставит ядро); env и таблица стартовых capability наследуются детям (`cap::endow` — наделение потомка без GRANT: новых полномочий не возникает, модель фабрик KeyKOS); процесс читает наследство `SYS_ARGS`/`SYS_STARTCAP` (преоткрытые права как preopen'ы WASI — раскладку стека контракт не фиксирует вовсе); posixfs: seek (i64+whence, зажим в [0,size]) и rename (корень+RAM-слот+каталог); vsh: `run NAME ARGS`, `mv`, `tail`, десятичный код выхода, эхо пачкой (лечит `<?>` на кириллице). Проверено на обеих архитектурах, 0 паник. См. [[process-contract]].

- [x] **Веха 31 — std-порт — закрыта**: таргеты `riscv64gc-unknown-void-elf` + `x86_64-unknown-void` (tier 3) и `target_os = "void"` в std форка (`vendor/rust`, ветка `void`, `0d7db89a`): pal/_start, stdio→SYS_WRITE/READ, args/env→SYS_ARGS, alloc = bump над ленивым SYS_MAP (dealloc no-op — MVP), Instant=rdtime/rdtsc, sleep/yield честные, thread_local=statik, HashMap работает (xorshift-сиды); fs/net/process/потоки — unsupported. Тулчейн: stage0 = свой rustup stable, LLVM внешний из nix (сабмодуль llvm-project НЕ нужен), stage1 ~40 мин один раз, `rustup toolchain link void`. Критерий превышен: hello-std (обычный Rust, 72–101 КиБ ELF) доставлен мостом и работает в vsh на ОБЕИХ архитектурах — argv/env/sort/HashMap/exit-код 7. Ядро: стек процесса 64 КиБ, куча 8 МиБ (std-ELF в куче дважды). См. [[std-port]].

- [x] **Веха 32 — uutils — закрыта** (первая честная точка самодостаточности [[0004-void-pkg]]): multicall coreutils 0.9.0 (`ls · cat · cp · wc · mv · rm · head · echo`) собран обычным cargo тулчейном `void` и работает в vsh с одного диска на обеих архитектурах; межарх-демо — `cp` на x86_64, `wc` того же файла на riscv64. std дорос до файлов: std::fs по IPC к posixfs (старт-cap слот 0), free-list аллокатор (bump похоронен), `os::fd`, SystemTime с фиктивной базой. Ядро: FP-контекст riscv (мина Вехи 31 обезврежена), стек процесса 256 КиБ, куча ядра 16 МиБ. Форк uutils — сабмодуль `vendor/coreutils` (ветка `void`, cfg-заплатки «как WASI» + патчи console/filetime); `sort` отложен (rayon/getrandom). См. [[uutils]].

- [x] **Веха 33 — политика коммитов store — закрыта** ([[group-commit]], реализация анализа [[commit-policy]]): формат v2 — индекс дельта-цепочкой с надгробиями в области объектов (коммит пишет O(изменений), не O(store); A/B-регионы не нужны; миграция v1 на лету); group commit — `set_root` копит `dirty_ops`, фиксируют порог/период в `resume()` и sync-on-idle в `wait_stdin()` (интерактивное окно потерь — ноль; загрузка целиком = +1 поколение вместо ~10); GC отделён от уплотнения — надгробия и счёт мусора, `compact()` двухфазный крах-устойчивый по порогу >1/2; хост-мост: `del` и `gc [--compact]`; boot-коммит писал ~4.6 МиБ — стало 4–5 КиБ. Лучший баг: gc чуть не съел roots-blob (носитель корней недостижим от корней), ядро маскировало сменой system root — пойман хост-демо.

- [x] **Веха 34 — virtio-net + сетевой стек — закрыта** ([[virtio-net]]): первый выход в сеть, микроядерность до конца — ядро отдаёт лишь сырые Ethernet-кадры (`virtio_net.rs`: две очереди RX/TX, опрос, 12-байтный `virtio_net_hdr`, MAC из конфига; арх ищет устройство — mmio id 1 / pci 0x1041, без MSI-X), а стек ARP+IPv4+ICMP echo живёт в userspace-сервере `net-srv` (284 строки). `cap::Device::Net` + шлюзы `SYS_NET_SEND/RECV/MAC` (набор дорос до 22); vsh получает эндпоинт сервера старт-cap слотом 2, команда `ping A.B.C.D`. Демо самодостаточно (QEMU SLIRP: гость 10.0.2.15, шлюз/DNS 10.0.2.2/.3 отвечают, не выходя из QEMU): самопинг на загрузке + `ping` из vsh на обеих архитектурах (riscv 204/218 мкс, x86 540/582 мкс), 0 паник. Отложено: IRQ, TCP/UDP, DNS.

- [x] **Веха 35 — потоки (std::thread) — закрыта** ([[threads]]): ядро планирует НИТИ внутри процесса (`Proc.group` — общие space/domain/куча лидера, свои кадр/стек/TLS); сисколлы `SYS_THREAD_SPAWN/EXIT/JOIN` + `SYS_FUTEX` (ключ space+uaddr) + `SYS_SET_TLS` (набор до 27), `SYS_EXIT` кладёт всю группу. Форк (`vendor/rust`): `has_thread_local`+local-exec, нативный TLS (tp Variant I / база %fs Variant II, шаблон `.tdata/.tbss` из линкер-скрипта), `Thread::new/join`, futex `Mutex/Condvar/RwLock/Parker`, аллокатор под спин-локом. Демо: `bin/threads` (no_std, счётчик 200000 под самодельным futex-мьютексом) и `bin/threads-std` (обычный `std::thread`+`Arc<Mutex>`+`thread_local!` с тестом изоляции TLS, 80000) — обе арх, паник 0. Лучший баг: x86 `fsbase` (MSR, не GP) затирался мусором на каждом трапе — фикс `carry_tls_from`. Разблокирует sort/rayon/ripgrep.

- [x] **Веха 36 — nixpkgs-cross (C-мир поверх void-libc) — закрыта** ([[nixpkgs-cross]], ступень 3 лестницы [[void-pkg]]): настоящие C-программы по рецептам nixpkgs — GNU hello (autotools+gnulib) и bzip2 — собираются `nix-build nix -A <arch>.<pkg>` и работают в vsh на обеих архитектурах (hello с UTF-8 `--greeting`, bzip2 честно сжимает/разжимает файлы персоналии). Тулчейн НЕ свой: pkgsCross.{riscv64,x86_64}-embedded (gcc 15.2 + newlib 4.5 из кэша), порт = глю `void-libc` (~610 строк: crt0 + стабы newlib поверх ABI VOID + specs, файлы — IPC к posixfs, sbrk — ленивый SYS_MAP) + общий linker.ld дорос до `.init_array`/`__global_pointer$`. Ядро: **FP/SSE-контекст x86** (CR4.OSFXSR, FxArea=fxsave64 в кадре, save_fp на трапе/restore_fp на входе — зеркало riscv-решения Вехи 32; до этого любой SSE-опкод C-кода давал #UD). Лучшие грабли: newlib riscv ждёт `_write`, newlib x86 — `write` (алиасы); `__structuredAttrs` прячет NIX_CFLAGS_COMPILE от cc-wrapper (класть в env.*); gnulib на неизвестной ОС (getprogname #error, getdtablesize→getrlimit) — функции в глю + ac_cv в configure + декларации в config.h.

- [x] **Веха 37 — checkpoint процессов — закрыта** ([[checkpoint]], наследие KeyKOS): вычисления переживают перезагрузку. `SYS_CHECKPOINT` (28, право WRITE) — процесс морозит СЕБЯ: страницы пространства → объекты store (дедуп: нулевые страницы стека схлопываются в один), манифест (кадр+heap_brk+args/env+таблица RWX-страниц) → node-объект, страницы — его дети (GC держит образ деревом от корня `proc/<arch>/<имя>`), коммит синхронный. Семантика setjmp: живому 0, размороженному 1. `SYS_RESTORE` (29, право EXEC, vsh `thaw`) — как exec: ждём код выхода; args/env из образа, старт-права — свежее наследство (капабилити сознательно НЕ консервируются). Контракт arch: `page_info` (VA→PA+MAP-флаги); thaw отвергает W+X и адреса вне региона (образу извне не доверяем). Демо `bin/freeze`: цикл с чекпойнтом на шаге 5 — «Я РАЗМОРОЖЕН: продолжаю с шага 5, куча цела ✓» ПОСЛЕ перезагрузки QEMU, итог 385, обе архитектуры, образы двух арх на одном диске. Отложено: многонитевые образы, заморозка чужого/заблокированного, глобальный периодический чекпойнт.

- [x] **Веха 38 — linux-abi (бэкенд B) — закрыта** ([[linux-abi]], исследовательская магистраль [[void-pkg]]): неизменённые static-PIE musl-бинари из кэша nixpkgs работают на VOID под персоналией Linux — трансля́тор Linux-syscall'ов **в ядре** (путь linuxulator). Ключевое: static-PIE (`ET_DYN`, `-static-pie`), а не ET_EXEC@0x400000 (тот на riscv течёт в общие подтаблицы, на x86 — внутрь образа ядра); musl `rcrt1` само-релоцируется, ядро лишь строит стартовый стек с **auxv**. Ни одной новой инструкции: riscv `ecall` общий с VOID (разводит `Proc.linux`), x86 `syscall` при EFER.SCE=0 даёт **#UD** → ловим вектор 6, `rip+=2` (без MSR/стаба). `brk`/`mmap(anon)` поверх ленивой кучи (Веха 22). Демо на ОБЕИХ архитектурах: `hello` (UTF-8 argv, exit-код) и **busybox 1.37** (`echo`/`uname -a`/`true`/`seq`/`printf`/`basename` — один бинарь, мультиплекс по argv). Отложено: файловые applet'ы (`cat FILE`/`ls`/`sh` — нужен мост к posixfs), clone/futex/сигналы. Грабли: cc-wrapper `-pie` ломает `ld -r` busybox'а (`NIX_HARDENING_ENABLE=""` + патч `ld_flags`), riscv-defconfig тянет x86-only SHA-NI.

- [x] **Веха 39 — WASI (бэкенд C) — закрыта** ([[wasi]], последний из трёх бэкендов [[void-pkg]]): неизменённые wasm32-wasi-модули работают через интерпретатор **wasmi** — тот же .wasm на ОБЕИХ архитектурах без пересборки. Микроядерно чисто: `bin/wasirun` — ОБЫЧНАЯ std-программа (тулчейн void, крейт wasmi 0.36 no_std), ядро не тронуто. Читает .wasm как файл (`std::fs` → posixfs), даёт импорты `wasi_snapshot_preview1` (fd_write→SYS_WRITE, proc_exit→SYS_EXIT, args из `std::env::args`). Демо: `run bin/wasirun hello.wasm` печатает из wasm-гостя, argv, exit-код — обе арх. Подпорки: posixfs 4 КиБ→128 КиБ (wasm 63 КБ не влезал), std-арена 16→64 МиБ (wasmi тесно). **Все три бэкенда ADR 0004 закрыты**: A (nixpkgs-cross), B (linux-abi), C (wasi) — `run` в vsh запускает четыре мира с одного диска (родной VOID-ELF, static-PIE musl, busybox, wasm).

- [x] **Веха 40 — декларативный init — закрыта** ([[declarative-init]], первая «фишка NixOS поверх родного фундамента» [[0004-void-pkg]]): система поднимается по КОНФИГУ-объекту из store, а не по зашитому `shell_session`. Конфиг — текст (`service NAME store:rw` / `shell vsh endpoint:posixfs store:xw env`); `init::apply` минтит права по токенам прямо в домены и разводит эндпоинты по именам сервисов — то, что раньше делал kmain руками, теперь из данных. Поколения = история корня, откат = смена `system/current`: vsh `switch GEN` + ребут грузит другую конфигурацию (gen1 полное / gen2 без сети — `ping` наглядно «сети нет»), и назад. Модель NixOS честно: язык Nix НЕ в рантайме — `nix/system.nix` вычисляет конфиг на ХОСТЕ (`nix eval` → мост → `system/gen3`), VOID грузит результат. Конфиг арх-нейтрален (init резолвит `bin/<arch>/*`) — один `system/*` на обе архитектуры. vsh `sysdef GEN FILE` регистрирует файл как поколение.

- [x] **Веха 41 — платформа: загрузка на реальном x86-железе — ПЕРВЫЙ ШАГ закрыт** ([[platform]], слой «платформа» из [[0005-multiarch-arch-boundary]]): x86-ядро грузится настоящей прошивкой (multiboot1/GRUB, не только QEMU-PVH), рисует на VGA (0xB8000 — у ноутбука нет COM-порта) и берёт размер RAM из карты памяти загрузчика (`platform_init` → `arch::ram_limit()` вместо зашитых 128 МиБ; direct-map/фреймы зажаты CAP'ом 256 МиБ). Проверено в QEMU через GRUB (тот же путь, что на металле): `-m 512M`→обнаружено 511, VGA-скриншот показывает загрузку до `vsh>`, без virtio-диска/сети (как на ноутбуке) НЕ падает — сеется в RAM-store, поднимается vsh. Сборка загрузочного USB: `Code/boot/mkboot.sh` → ISO → dd. **Металл проверяет владелец платы** (у ассистента её нет). НЕ поедет с первого раза: клавиатура (нужен PS/2/USB-HID → на `vsh>` замирает), диск/сеть (AHCI/реальный NIC вместо virtio), кириллица на VGA (`?`), аллокатор по memmap.

- [x] **Веха 42 — драйвер клавиатуры (PS/2) — закрыта** ([[platform]], добита на реальном железе ASUS X54C): `vsh>` стал ИНТЕРАКТИВНЫМ на металле. `ps2.rs` — контроллер 8042 (внутренняя клавиатура ноутбука висит на нём как PS/2): скан-коды набора 1 из порта 0x60 → ASCII (Shift/CapsLock, US) → то же кольцо консоли, что и COM1 (`rx_push`). Наполнение: опрос на тиках + IRQ1 через IOAPIC на общий вектор консоли (IRQ будит из сна `wait_stdin`). Проверено в QEMU через `sendkey`: набрано `run bin/hello` — vsh отэхоил, запустил, код 0; кириллица-VGA (Веха 41) + клавиатура = живой русский терминал на реальном экране. Отложено: расширенные клавиши (стрелки), USB-HID, русская раскладка.

- [x] **Веха 43 — полировка TTY/vsh («косметика») — закрыта** ([[tty]], запрос пользователя после запуска на X54C): консоль перестала «выглядеть так себе». Единый механизм — vsh печатает **ANSI-коды**, а VGA-ядро (`vga.rs`) их разбирает (SGR-цвета, `ESC[2J` clear, `ESC[H/K`) и переводит в атрибуты VGA, не рисуя байты; на serial те же коды толкует терминал → цвет/`clear` работают ОДИНАКОВО в QEMU и на реальном экране. Аппаратный курсор VGA теперь двигается за письмом (`sync_cursor`, CRTC 0x0E/0x0F) — не «висит вверху». vsh: `clear`, цветной табличный `help` (жёлтые команды + русские описания), зелёное приглашение. `cd` осознанно отложен — posixfs ПЛОСКАЯ (нет каталогов/cwd).

- [x] **Веха 44 — папки в posixfs (иерархия) — закрыта** ([[folders]], первый пункт [[known-gaps]]): у персоналии появились каталоги/пути/`cwd` БЕЗ правок ядра. Файл — корень `f<путь>`, каталог — индекс-корень `d<путь>` (оба GC-safe); голое имя → `/<имя>` (std-программы работают без пересборки). vsh: резолвер путей (`.`/`..`/абс/отн), приглашение с каталогом (синим), команды `cd`/`pwd`/`mkdir`/`rm` + путь-осознанные `ls [DIR]`/`cat`/`tail`/`echo`/`mv`. Проверено на обеих архитектурах, персистентно (вложенные каталоги переживают перезагрузку). Осталось: прав/времён нет, `rm -r` нет, std-`create_dir` нет.

- [x] **Веха 45 — редактирование строки в vsh — закрыта** ([[tty]], пункт [[known-gaps]]): приглашение стало настоящим редактором — курсор в строке, вставка/удаление в позиции, `←`/`→`/`Home`/`End`/`Delete`, история команд `↑`/`↓` (кольцо на 8). Единый ANSI-механизм: PS/2 (`ps2.rs`) шлёт расширенные клавиши (`0xE0`) как `ESC[…`, terminal QEMU — те же коды, VGA-ядро разбирает (+ добавлено перемещение курсора `ESC[nA/B/C/D`), vsh парсит и перерисовывает строку (позиция по границам символов UTF-8). Проверено: serial (вставка в середине/backspace/история) и VGA через PS/2 (`sendkey up` поднял команду из истории). Ограничение: строки шире экрана редактируются неидеально.

- [x] **Веха 46 — освобождение фреймов — закрыта** ([[frame-freeing]], техдолг #3 из [[known-gaps]]): `frame.rs` дорос до списка свободных (интрузивный Treiber-стек, переиспользование вместо чистого bump); `arch::free_address_space` сносит приватные фреймы пространства (страницы+таблицы+корень), узнавая общие с ядром узлы сравнением с `KERNEL_ROOT` (x86 — с учётом клона PDPT под PML4[0]); `proc::reclaim_dead_spaces` в `resume()` освобождает группу, когда ВСЕ её нити `Finished` (единая точка для exit/exit_group/фолт-килла), предварительно переключив satp/CR3 на живого преемника (нельзя рушить активные таблицы). Проверено: обе арх — демо без OOM, ~327 фреймов вернулись; `run hello` ×12 → пик `[mem]` не сдвинулся (течь остановлена).

- [x] **Веха 47 — драйвер AHCI (SATA) — закрыта** ([[ahci]], первый «настоящий» драйвер железа из [[known-gaps]]): store читает/пишет на реальный SATA-диск в режиме AHCI — тот же интерфейс `BlockIo`, что и virtio-blk. `arch::probe_ahci` (x86) сканирует PCI-шину со ВСЕМИ функциями (на Intel-PCH контроллер на 00:1f.2), берёт ABAR/порт с диском; `ahci.rs` — опросный драйвер (список команд + FIS + PRDT, READ/WRITE DMA EXT, IDENTIFY для ёмкости); `object.rs` диспетчеризует носитель (AHCI → иначе virtio-blk). Проверено в QEMU (ich9-ahci без virtio): store прочитан по AHCI (32768 секторов, все программы), `echo > f` пережил ребут (`cat` вернул текст), регрессия virtio чиста на обеих арх. ⚠️ на реальном ноуте пишет в СЫРОЙ диск (сектор 0) — сотрёт существующую ОС; только пустой/запасной диск.

- [x] **Веха 48 — установка на диск — закрыта** ([[install]], live-USB → install → загрузка с диска): store переехал с сектора 0 в раздел (AHCI читает MBR, `base` LBA; тип раздела `0x9f`), `Code/boot/mkdisk.sh` собирает загрузочный образ (GRUB i386-pc вручную — `grub-bios-setup` на NixOS падает; p1 FAT с ядром, p2 store). Образ едет модулем multiboot2 на install-ISO (`arch::boot_module`, уберегаем от аллокатора фреймов) — установленное ядро == работающему. vsh `install` → `SYS_INSTALL` (№30, гейт store WRITE): пишет префикс на диск, растягивает p2 на весь диск, чистит store-область, замораживает кэш. Проверено end-to-end в QEMU: чистый диск → install → ребут только с SATA → VOID грузится, пишет, переживает ещё ребут. ⚠️ install СТИРАЕТ весь диск.

- [x] **Веха 49 — драйвер Intel e1000 (NIC) — закрыта** ([[e1000]], настоящая сеть на металле): драйвер PRO/1000 (PCI 8086:100e/10d3, BAR0 MMIO, RX/TX-кольца дескрипторов, MAC из RAL/RAH или EEPROM, опрос) даёт net-srv те же сырые кадры, что virtio-net; носитель диспетчеризуется `net.rs` (e1000 ↔ virtio-net, зеркало AHCI/virtio-blk). Проверено в QEMU (`-device e1000`): `ping 10.0.2.2` отвечает через e1000; регрессия virtio-net (обе арх) чиста. ⚠️ у X54C проводной порт Atheros/Realtek — не e1000; e1000 — первый общий NIC, не финал для этого ноута.

- [x] **Веха 50 — USB xHCI + HID-клавиатура — закрыта** ([[usb]], самый большой драйвер, 3 части): (A) контроллер — сброс/DCBAA/кольцо команд/кольцо событий/запуск/порты, Enable Slot проверяет машинерию; (B) перечисление — Address Device (input-контекст, EP0 TR-кольцо), control-трансферы по EP0, GET_DESCRIPTOR; (C) HID — config-дескриптор→interrupt-эндпоинт, SET_CONFIGURATION/SET_PROTOCOL(boot)/Configure Endpoint, poll_hid (из console_drain) парсит boot-репорты→ASCII→консоль; таймер не маскируется при простое, когда USB-клава поднята (у USB нет прерывания). Проверено в QEMU (qemu-xhci+usb-kbd, QMP send-key): набор «pwd»→Enter выполнился. ⚠️ X54C=Sandy Bridge→EHCI, не xHCI — этому ноуту не поднимет.

- [x] **Веха 51 — фундамент userspace-драйверов — закрыта** ([[userspace-drivers]], слой 1 к Linux-драйверам): capability-цели `Mmio{base,len}`/`Dma`; `SYS_MMIO_MAP` (маппит регистры устройства в процесс) + `SYS_DMA_ALLOC` (DMA-страница + физ-адрес); `frame::is_ram`-guard при сносе простора (не освобождать MMIO-листья); токены init `mmio:`/`dma`; демо `bin/e1000d` — Intel e1000 В USERSPACE (замапил BAR, прочитал MAC по MMIO, отправил кадр — карта выставила DD). Проверено в QEMU (virtio-net ядру + e1000 драйверу). Осталось из фундамента: доставка IRQ в userspace.

- [x] **Веха 52 — прерывания в userspace — закрыта** ([[irq]], второй и последний механизм фундамента [[userspace-drivers]]): драйвер спит до прерывания своего устройства вместо опроса. Capability-цель `Irq{vector}`; отдельный IDT-вектор `VEC_USERDRV=35` (обработчик НЕ берёт замок таблицы процессов — только атомарный флаг `USERDRV_IRQ_PENDING` + EOI); `SYS_IRQ_WAIT` (№33) → `State::IrqWait`, `drain_userdrv_irq` будит ждущих в `resume`/`wait_stdin`. Тонкость IOAPIC: QEMU-e1000 без MSI → PCI INTx **level-triggered active-low** (`route_level_low`: polarity/trigger-биты) + **oneshot-маска** (пины стартуют замаскированными; `SYS_IRQ_WAIT` взводит через `userdrv_irq_arm`, обработчик снова маскирует — как threaded-oneshot IRQ в Linux, без шторма). Демо `bin/e1000d`: размаскировал причину в IMS, дёрнул её через ICS, уснул в `SYS_IRQ_WAIT`, проснулся от реального прерывания карты (`ICR=0x04`=LSC). Проверено в QEMU; riscv — заглушки (`userdrv_irq_arm`/`e1000_irq_setup`), загрузка без регрессий.

- [x] **Веха 53 — lx_emul: первый каркас Linux-API шима — закрыта** ([[lx-emul]], верхний слой к хостингу Linux-драйверов): драйвер пишется «как в Linux», шим переводит вызовы в syscall'ы VOID (Genode `dde_linux`-стиль, каркас на Rust). `programs/user/src/lx_emul.rs`: `ioremap`+`readl`/`writel` (→`SYS_MMIO_MAP`), `dma_alloc_coherent`→`{cpu,dma}` (→`SYS_DMA_ALLOC`), `request_irq(handler)` — нить (`SYS_THREAD_SPAWN`), крутящая `SYS_IRQ_WAIT` (= Linux threaded-oneshot IRQ, ровно наш IOAPIC-oneshot), `kmalloc`/`kzalloc`/`kfree` (свой bump+free-list поверх `SYS_MAP`), `Completion` (futex), `module_init(probe)`+`Device` (собирает mmio/dma/irq cap из стартовых прав). Демо `bin/lx_e1000` — e1000 как Linux-СТИЛЕВОЙ драйвер (init спавнит его вместо `e1000d`): `ioremap`→сброс/линк→`dma_alloc_coherent` кольцо+буфер→TX (DD)→`request_irq`+`wait_for_completion`, проснулся от реального IRQ карты (threaded-нить, `ICR=0x04`). Проверено в QEMU (обе нити чисто вышли по `SYS_EXIT`, vsh поднялся); riscv — компилируется и сеется, не спавнится (нет e1000), без регрессий. Обе арх: 0 предупреждений.

- [x] **Веха 54 — lx_emul C-путь: C-драйвер против C-шима — закрыта** ([[lx-emul-c]], продолжение [[lx-emul]]): драйвер на C, скомпилированный кросс-gcc против void-libc, работает на VOID и гонит e1000 Linux-подобным API — тот же тулчейн, что для реальных `.c` из Linux. `void-libc/lx_emul.{c,h}` (в libvoid.a): `lx_emul.h` — чистый Linux-API без syscall.h (ioremap/readl/writel/dma_alloc_coherent/kmalloc/request_irq/completion/printk/module_init); `printk(fmt,…)` через newlib vsnprintf; `request_irq` — нить (SYS_THREAD_SPAWN) на SYS_IRQ_WAIT; kmalloc → newlib malloc; completion → futex. `syscall.h` дорос драйверными обёртками. `programs/lx-cdriver/lx_e1000.c` — e1000 как Linux-стилевой C-драйвер (кириллица в printk свободно — C-литералы UTF-8). Сборка: `nix-build nix -A <arch>.lx_e1000` (кросс-gcc оффлайн из локального стора) → мост `void-store-import put … bin/<arch>/lx_e1000_c` → init спавнит C-драйвер, если импортирован, иначе Rust `lx_e1000`. Проверено в QEMU x86: ioremap→SYS_MMIO_MAP, printk %02x→MAC, dma_alloc_coherent+writel→DD, request_irq(threaded C-нить)+wait_for_completion→реальный IRQ (ICR=0x04), SYS_EXIT рубит обе нити, vsh поднялся. riscv — компилируется и импортирован, не спавнится (нет e1000), без регрессий. Обе арх: 0 предупреждений. C-ELF на диске (void-disk.img в .gitignore), воспроизводится nix-build+мост.
## Опционально / исследовательское
- [ ] SASOS-эксперимент: изоляция средствами Rust вместо железных границ (см. [[0001-rust-riscv-microkernel]]). Практичный первый шаг — гибрид: доверенные серверы в одном AS (модель Theseus/Hubris), код сервера = проверяемый объект по хэшу. См. [[phase-2-state-analysis]].
