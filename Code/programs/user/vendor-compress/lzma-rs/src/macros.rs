// Апстримные макросы логирования. Ветка с крейтом `log` убрана: у программ VOID нет логгера,
// а `log` тянул бы ещё одну зависимость в крейт, который сознательно оставлен без единой.

macro_rules! lzma_trace {
    ($($arg:tt)+) => {};
}

macro_rules! lzma_debug {
    ($($arg:tt)+) => {};
}

macro_rules! lzma_info {
    ($($arg:tt)+) => {};
}
