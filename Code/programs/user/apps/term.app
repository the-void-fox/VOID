# Ярлык программы `term` (Веха 146.1): данные о ней ЛЕЖАТ РЯДОМ С НЕЙ, в store под корнем
# `app/<арх>/term`. Их читает строка запуска; сама программа о них не знает.
name Терминал
info Оболочка системы в окне
