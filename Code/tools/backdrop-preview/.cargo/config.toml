# Хост-утилита: собирается под хост, а не под таргеты ядра. Этот конфиг ПЕРЕКРЫВАЕТ
# build.target из Code/.cargo/config.toml — ближний к крейту побеждает.
[build]
target = "x86_64-unknown-linux-gnu"
